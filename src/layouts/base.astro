---

import Header from "../components/Header.astro"

const { metaTitle = '', lang= '', metaDescription = '', article = '', absoluteUrl = '', metaImage = '', author = '' } = Astro.props;
const YearToday = new Date();
---
<html lang="en">

<head>
  <title>{metaTitle}</title>
  <meta lang={lang} />
  <meta charSet="utf-8" />
  <meta name="viewport" content="initial-scale=1.0, width=device-width" />
  {metaDescription &&
  <meta name="description" content={metaDescription} />}
  <meta name="og:title" content={metaTitle} />
  <link rel="icon" type="image/svg+xml" href="/favicon.ico">
  {metaDescription && (
  <meta name="og:description" content={metaDescription} />
  )}
  <meta name="og:type" content={article ? "article" : "website" } />
  {metaImage &&
  <meta name="og:image" content={metaImage} />}
  {metaImage &&
  <meta name="og:image:alt" content={metaTitle} />}
  <meta name="og:url" content={absoluteUrl} />
  <meta name="twitter:card" content="summary_large_image" />
  {author &&
  <meta name="twitter:creator" content={author} />}
  <meta name="twitter:title" content={metaTitle} />
  {metaDescription && (
  <meta name="twitter:description" content={metaDescription} />
  )}
  {metaImage &&
  <meta name="twitter:image" content={metaImage} />}
  {metaImage &&
  <meta name="twitter:image:alt" content={metaTitle} />}
  <link rel="stylesheet" href={Astro.resolve("../styles/global.css")}> </head> 
  <body class="bg-lightDark">
    <Header />
    <main>
      <slot />
    </main>
    <!-- Before Footer -->
    <section class="flex flex-col gap-12 items-center w-full text-white">
      <h2> AHORA QUE YA NOS CONOCES UN POCO... </h2>
      <div>
        <p class="text-center stroke-text hero-paragraph">
          ¿Te ayudamos a potenciar tu negocio online?
        </p>
        <p class="text-center"> 
          Tenemos por objetivo generar el máximo retorno de inversión (ROI) para tu negocio. Somos muy transparentes en <br />  
          nuestros procesos y siempre estarás informado de como gestionamos tu campaña y los resultados obtenidos.
        </p>
      </div>
      <button class="bg-white text-lightDark px-16 py-9 text-4xl rounded-lg font-bold">
        ¿Háblanos de tu proyecto?
      </button>
    </section>
    <footer class="w-full text-white font-centuryGothic font-bold">
      <div class="flex flex-row mb-20 justify-between">
        <div class="flex flex-col gap-2 text-5xl">
          <a href="mailto:hola@zegura.agency" class="underline mb-11">Hola@zegura.agency</a>
          <div> Acceso al periférico 22 norte #814</div>
          <div>Puebla, Pue 72810 </div>
          <a href="tel+522226772264"><span>+52 222 677 2264</span></a>
        </div>

        <div class="flex flex-col gap-7">
            <h5>Redes Sociales</h5>
        </div>
      </div>

      <div class="flex justify-end text-5xl">
          <span>© {YearToday.getFullYear()}</span>
      </div>
    </footer>
  </body>
</html>